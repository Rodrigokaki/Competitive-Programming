#include <bits/stdc++.h>
using namespace std;

#define endl "\n";

void solve(){
    map<string, array<long long, 3>> cavalos;
    string nome;
    long long t1, t2, t3;

    while(cin >> nome){
        cin >> t1 >> t2 >> t3;
        cavalos[nome] = {t1, t2, t3};
    }

    cout << "T1 ";

}


int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t;
    t = 1;
    while(t--){
        solve();
    }

    return 0;
}
